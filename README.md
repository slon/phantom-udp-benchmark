phantom-udp-benchmark
=====================

Phantom module for udp load testing. See example/phantom.conf for example phantom config.

Accepts ammo in raw phantom ammo format, writes standard phantom logs.
